你知道的排名函数有哪些？说一说它们之间的区别？
1. row_number() over()
   没有并列名次，名词连续，如1，2,3,适合用于生产主键或者不并列排名
2. rank() over()
   有并列名次，名次不连续，如1，1,3
3. dense_rank() over()
   有并列名次，名次连续，如1,1,2

区别：
    1. 共同点： 三者都可以用于分组排名，名次都是递增。
    2. 不同点： row_number()没有并列名次，名次连续，rank()有并列名次，名次不连续
               dense_rank() 有并列名次，名次连续

